<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Fabrics</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/"><b>Fabrics</b></a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/home">Home</a>
              </li>
            </ul>  
              <br></br>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addProduct">Add Product</a>
              </li> 
            </ul>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
              <ul class="navbar-nav">
                <li class="nav-item">
                  <a class="nav-link" href="/cart">Shopping Bag</a>
                </li> 
              </ul>
          </div>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2><b>Update Product</b></h2></div>
        <br>
        <div class="text-center">
          <% if (products) { %> 
            <form action="/editProduct/<%= products.productid %>" method="POST">
                <label for="\ID"><h4><b>Product ID:</b></h4></label><br>
                <input type="number" id="ID" name="ID" min="0" step="1" value = <%= products.productid %> required><br><br>
              
                <label for="name"><h4><b>Product Name:</b></h4></label><br>
                <input type="text" id="name" name="name" value = <%= products.productname %> required><br><br>

                <label for="price"><h4><b>Product Price:</b></h4</label><br>
                <input type="number" id="price" name="price" min="0" step="0.01" value = <%= products.productprice %> required><br><br>

                <label for="image"><h4><b>Image</b></h4</label><br>
                <img src = "<%= products.image %>" width="20%"><br><br>

                <button class="btn btn-primary" type="submit" value="Submit" stlye="text-center"> Update Product</button>
            </form>
          <% } else { %>
              <p>No product found.</p>
          <% } %>
          <a href ="/"><h5>Back</h5></a>
        </div>
    </div>
</body>
</html>
